import React, { useState } from "react";
import MainView from "./views/MainView";

const App: React.FC = () => {
    return (
        <div>
            <MainView />
        </div>
    );
};

export default App;
